// cinema4d_mcp_helper — C++ companion plugin for the cinema4d-mcp Python plugin.
// Closes the two Python-API gaps in Scene Nodes capsule authoring:
//   1. GraphModelInterface::AddPort(parent, Id name) for named FIO ports
//   2. NodeTemplate-typed asset publishing (.c4dnodes format)
//
// PHASE A.1 (2026-04-30): adds graph/nodes/nodespace frameworks for the
// AddPort wrapper. Phase A.0 minimal-build verified loadability; A.1
// adds the actual Scene Nodes primitives.
//
// Layout matches the user's existing working plugins (Luminary, MechFlow,
// Spikr2) — same Platform / stylecheck / ModuleId convention to avoid
// build-pipeline surprises.

Platform=Win64;OSX;Linux

Type=DLL

APIS=\
cinema.framework;\
cinema_hybrid.framework;\
core.framework;\
math.framework;\
misc.framework;\
mesh_misc.framework;\
graph.framework;\
nodes.framework;\
nodespace.framework

C4D=true

stylecheck.level=0

ModuleId=net.sdimaging.cinema4d_mcp_helper
