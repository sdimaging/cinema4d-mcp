// Symbol header for cinema4d_mcp_helper. Phase A.0 has no UI / description
// resources — the plugin is a pure MessageData receiver. This file exists
// because the C4D resource system expects it during PluginStart()'s
// g_resource.Init() call.

enum
{
    _DUMMY_SYMBOL_ = 0,
};
